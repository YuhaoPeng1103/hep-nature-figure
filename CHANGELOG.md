# 变更记录

## v2.6.2 — 2026-09-26

**用 UPC 算例（`fig_upc_run3/`）把默认路线③端到端重跑一遍**，抓出 **5 个真问题**。
其中第 1 个是**物理错误从闸口漏过去了**：新出的一批位图两核的 Lorentz 收缩
方向全部画反，而旧的几何约束四条全部"通过"。

### 修 1（★ 最严重）：`style.conventions` 从来没进过简报 —— 两核压扁方向画反

IR 里白纸黑字写着

```yaml
style:
  conventions:
    - "核必须画成纵向压扁的椭圆（Lorentz 收缩）"
```

但 `ir_to_genbrief.py` 只把 `elements` 倒进简报，`style.conventions`
**一个字都没带**。IR 说了、没人执行 —— 等于没说。实测后果：

| 图 | 核A 高/宽 | 核B 高/宽 | 判定 |
|---|---|---|---|
| 参考图 T3-33（物理正确） | 4.30 | 4.28 | ✅ |
| run2 草图 `sketch_s11` | 0.56 | 0.65 | ❌ 横扁 |
| run2 成品位图 `render_s31` | 1.69 | 0.78 | ❌ 一半画反 |

两核沿**水平**束流运动，收缩必须沿水平方向 ⇒ 画成高瘦椭圆。横扁 = 收缩轴
垂直于速度 = 物理错。修完（简报新增「三·补、★ 形态约定」）后 3/3 张草图
全部正确：1.42/1.53、2.45/2.68、1.44/1.53。

顺带把 IR 里那句有歧义的 `primitive: 竖直扁椭圆` 改成
`高瘦椭圆（长轴竖直；沿水平束流方向压扁 = Lorentz 收缩）` —— 模型把
「竖直扁椭圆」读成「压扁在竖直方向」，这正是一半的成因。

### 修 2：闸口缺一条「单个形体朝向」的机器判据 —— 补上

旧的 `geometry_constraints.约束` 四条（相向 / 不重叠 / 光子线在两核之间 /
末态背对背）**全是「核与核之间」的关系，没有一条管单个形体的朝向**，
所以上面那两版错图都过了闸口。

新增 `geometry_constraints.机器` + `check_sketch.py` 的 `lorentz_check()`：

1. 核物质是整张图里**唯一的大面积彩色对象**（核子气是红/绿/蓝小球；
   其余元素都是深色线、箭头、文字）→ 用饱和度取大块；
2. 单个核子之间有缝，连通域会是几百个小圆 → 先膨胀合并，再取**未膨胀**
   像素的 bbox（否则框被结构元素撑大 2k px）；
3. 束流方向由 IR 声明（`束流方向: horizontal|vertical`）→ 要求高/宽 ≥ 阈值。

三条实测（`assets/ir/sketch5_upc.ir.yaml`）：

```
T3-33 参考图   ✅ 核#1 高/宽=4.30   ✅ 核#2 高/宽=4.28
run2 草图 s11  ❌ 核#1 高/宽=0.56   ❌ 核#2 高/宽=0.65   → 阻断
run2 位图 s31  ❌ 核#1 高/宽=0.78   ✅ 核#2 高/宽=1.69   → 阻断
```

### 修 3：成品位图那步**没把草图当构图依据传进去**

SKILL.md 第 5 步只写 `--ref refs/T3-33.png`（风格参考图）。于是模型**重新猜了
一遍构图** —— 本次 UPC 算例它把核 B 画成横扁，而它拿到的草图里是高的。
「根据草图生成位图」这句话在流程里是空的。

修：SKILL.md 与简报（`stage == "render"`）都要求
`--ref <上一步选中的草图.png> --ref <风格参考图.png>`。改后成品位图
（`render_s22`，核 1.81/1.84）与草图几何一致。

### 修 4：IR 的 `primitive` / `params` 整段被丢掉

`ir_to_genbrief.py` 只用 `primitive` 做了分类，**从没打到纸面上**，
`params` 也一样。后果：IR 写「弹簧线（螺旋）」→ 模型画一根虚线；
「射线数 11」→ 画一个实心点；「长宽比 约 2.2:1」→ 形同没写。

修：新增 `shape_hint()`，每条元素后补 `【形态：<primitive>；k=v, …】`
（坐标类键 cx/cy/x/y/r/R/rx/ry 排除 —— 那由「二、构图」负责）。
改后草图真的画出了**两条弹簧光子线**和**星芒顶点**。

### 修 5：`primitive: 文字` 的标签不进简报

`ANNOT_PRIMS` 原来只认英文 `"label"`。UPC 那条 IR 写的是 `primitive: 文字`，
于是**标签文字整段没进简报**，模型自己编 —— 编出来是中文「核A / 核B」，
而 IR 要的是 `A` / `B`。

修：`ANNOT_PRIMS = {"label", "text", "文字", "标注"}`；取值改成
`params.t or e["text"]`（IR 里常写顶层 `text:`）；并加一句
「**逐字照写上面这些字符串**（大小写、上下标 e⁺ / e⁻ 都要对），
不要翻译成中文、不要自己加标签」。

### 修 6：`check_sketch.py` 的「内容贴边/出界」假阳性

束流线 / 参考线**本来就该跑到画布外**（参考图 T3-33 就是：两条虚线一直顶到
左右边）。旧版一律报「内容贴边/出界 —— 会被裁」，实测**连参考图自己都过不了**。

修：新增 `edge_frac` —— 贴边那一圈（max(4px, 2%) 宽）里非背景像素的占比。
细线 0.5~2%，被裁的实心块几十 %，中间取 10%：
≤10% ⇒ ⚠️ 软提醒「看着是线条本来就该出画布」，>10% 才 ❌ 阻断。

实测：`sketch_s13` left/right 各 0.6% ⇒ ⚠️（旧版 ❌）。
T3-33 因为核离左边只有 1% ⇒ 仍 ❌（这条不是假阳性，1% 留白本来就太窄）。

### 修 7：`audit_composition.py` 的「出界」假阳性 —— 零面积毛边

实测 `fig_upc_run3/upc_q16.pdf`（Edge print-to-pdf）：

```
③ 出界/贴边: 2 处
   ❌ 图形 461×289pt @ (0,0)
   ❌ 图形 476×288pt @ (43,2)
```

看着像"盖住大半个页面还出界"。真相：`groupvec` 会把**同色矩形并成一条 path**
（体积优化），bbox 是全体子路径的并集。那第一条 path 有 **396 条子路径，
只有 3 条越界**，越界部分的并集是 `(0.63, 289.19, 0.63, 289.50)` ——
**宽为 0**，面积 0（Edge 像素对齐的 hairline）。第二条同理，越界并集是一条
竖直线段。拿并集 bbox 判 =「一处贴边 ⇒ 整条 path 出界」。

修：按 **subpath（item）** 判，并要求**越界部分的面积 > 0.5 pt²**。
现在报的是越界那一块的尺寸，还会说清几条子路径越界：

```
③ 出界/贴边: 0 处  （另有 2 条零面积毛边被忽略）
```

顺带修掉 `_item_rect` 里一个潜在崩溃：PyMuPDF 的 item 里夹着**整数**
（如 `("re", Rect, 1)` 的方向位），原来用 `q is not None` 判断会
`AttributeError`，改成 `hasattr(q, "x")`。

### 修 8：`evals/test_tools.py` 在中文 Windows 上 2 个假失败

`subprocess.run(..., text=True)` 用 locale 编码（GBK）解码子进程的 UTF-8 输出
（子进程继承了 `PYTHONIOENCODING=utf-8`）→ 读线程 `UnicodeDecodeError`
→ `r.stdout` 变成 `None` → 测试报 `argument of type 'NoneType' is not iterable`。
4 处调用显式加 `encoding="utf-8", errors="replace"`。

### 回归

`evals/test_tools.py`：**11/13 → 14/14**（新增 1 条
`composition_oob_ignores_zero_area_sliver`，实跑真 PDF 验判据；另 2 条是上面
修 8 从假失败转真通过）。

### run3 交付数字（1664×928）

| 阶段 | 产物 | 指标 |
|---|---|---|
| 草图 | `gen/sketch_s7.png` → `gen/sketch_s7.svg` | MAE 0.861 / >8 0.20% / 31217 `<path>` / 6 `<text>` / 自检 ✅ |
| 成品位图 | `gen/render_s22.png`（seed 22，双参考图） | 闸口② ✅（核 1.81/1.84，无边框） |
| 矢量 | `upc_q16.svg` | 2.67 MB / 5396 `<path>` / **9 `<text>`** / **0 `<image>`** / MAE 0.897 / >8 0.30% / >32 0.23% / PSNR 30.83 dB |
| 交付 | `upc_q16.pdf`（183×102 mm） | `check_delivery` ✅ 纯矢量 + 最小字号 9.2 pt；`audit_composition` ✅ ①0 ②0 ③0 ④0/9 |

物理核对（看图逐条）：两核相向 ✅、两核不重叠 ✅、两核**沿束流压扁** ✅、
两条准实光子弹簧线在两核之间 ✅、γγ 顶点星芒 ✅、e⁺/e⁻ 背对背 ✅。

## v2.6.1 — 2026-09-26

**拿到一把可用的 DashScope key 后，把 `gen_figure.py` 的**联网路径**真跑了一遍**
—— 结果连接找到 **3 个只有真调不会暴露的 bug**（`--dry-run` 全部绕过了它们）。
另外修掉一个“文档里命令就是坏的”的 OCR 参数。

### 实测记录（`gen/calls.jsonl`）

| # | 阶段 | seed | 结果 |
|---|---|---|---|
| 1 | sketch | 1 | ❌ 提交 400：`Image must be either a public URL or a Base64 encoded string` |
| 2 | sketch | 1 | ✅ `sketch_s1.png` 537 KB |
| 3 | sketch | 3 | ✅ `sketch_s3.png` 328 KB |
| 4 | render | 22 | ✅ `render_s22.png` 534 KB |

四次都是 1664×928 + 1 张参考图，模型 `qwen-image-3.0`（28~99 s/张）。

### 修 1：`upload_ref()` 拿不到 OSS 字段（KeyError）

`getPolicy` 的响应字段是 **`oss_access_key_id`**，而不是 `access_key_id`；
而且响应里**没有现成的 `key`** —— 它只给 `upload_dir`，policy 的条件是
`starts-with $key <upload_dir>`，所以 object key 必须自己拼 `upload_dir/<文件名>`，
否则 OSS 403 拒收。修前：`KeyError: 'access_key_id'`。

### 修 2：mm 端点**不认 `oss://`** —— 改走 base64 data URI

修完修 1 上传成功，但提交直接被拒：

```
400 InvalidParameter: Image must be either a public URL (http:// or https://)
or a Base64 encoded string (data:{mime_type};base64,{data})
```

所以 `upload_ref()` 整块换成 `ref_to_image_field()`：本地图 → base64 data URI（公网 URL
直接透传）。**OSS 临时上传那整段代码删掉了**（多一个环节就多一个
失败点，而且它本来就是错的）。

> 两个 bug 都在“先上传、再提交”这条路上，`--dry-run` 不发请求，
> 所以**之前的“已自检”完全没覆盖到**。

### 修 3：`ocr_words.ps1` 没有 `-Out` 参数（文档里的命令是坏的）

`SKILL.md` / `README.md` / `sketch_to_vector.py` 三处都写着
`ocr_words.ps1 -Image fig.png -Out words.txt`，但脚本只有 `-Image` / `-Lang`，
输出走 stdout。于是报 `parameter name 'Out' is ambiguous`。

更坏的是 `try_ocr()` 是 `except: return None`，**把报错吞了**，使用者只看到
“没有词表”、反复加 `--ocr` 也无济于事（而且提示语还在说“要真 `<text>` 就加 --ocr”）。

修法：脚本补 `-Out`（UTF-8 无 BOM）；`try_ocr` 把 returncode + stderr 打出来；
`--ocr` 失败时的提示语与“压根没加 --ocr”分开。
实测：同一张草图现在能出 **11 行**词表（“核 / A / 超 / 周 / 边 / 碰…”）。

### 修 4：草图简报太宽松 —— 模型给你一张**渲染稿**，根本不是草图

旧简报只说“不要求质感”“平涂…也行”—— “也行”就是可以画得很好看。
结果出来的“草图”带全身高光形渐变，自动切图层直接爆掉（**170657 条 `<path>` / 16.4 MB**）——
“人可直接改”这个前提根本不成立。

简报改成**强制扁平矢量风**（平涂纯色 / 禁止渐变阴影高光反射 /
禁止 3D 立体感和材质纹理 / 每个元素只画一层形状），并把“为什么”
（下一步要自动切图层）一并写进去。

| 同一个 seed 水位（R=12） | 渲染稿 | **扁平稿** |
|---|---|---|
| `<path>` | 170657 | **30412** |
| 物理元素 | 247 | **40** |
| 体积 | 16.40 MB | **3.26 MB** |

### 新增：`--q` 颜色量化 —— 草图路径数再降 14 倍

上面剩下的 3 万条路径不是色差造成的（`--R` 从 12 调到 40 只降到 27536），
而是画面边缘的**抗锯齿过渡带**—— 四叉树会沿着较缓的颜色斜坡细细切下去。
新增 `--q L`（每通道量化成 L 级），把过渡带塔缩成 1~2 个硬边。

实测（同一张扁平草图 `sketch_s3.png`，1664×928）：

| `--q` | `<path>` | MAE | >8 色阶像素 | 体积 |
|---|---|---|---|---|
| 0（不量化） | 30412 | 0.376 | 0.32% | 3.26 MB |
| 6 | 873 | 3.210 | 16.92% | 0.80 MB |
| 10 | 1255 | 1.273 | 7.77% | 0.88 MB |
| **16（新默认）** | **2111** | **1.102** | **0.31%** | **1.00 MB** |

`q=16` **路径数降 14.4 倍，而 >8 色阶的像素占比和不量化持平**（0.31% vs 0.32%）——
这是“既给人改、又不比位图差”的那个点。`sketch_to_vector.py` 默认 16；
`raster_to_vector_semantic.py` 默认 **0**（不量化，保位图临摹原样）。

> 注意：若草图里本身留有轻微明暗（如本例 chunkB 内部的淡斑），
> 量化会把它变成杂斑。要干净就再压一次简报（要求绝对平油），或 `--q 0`。

### 回归

`raster_to_vector_semantic.py` 跑 UPC（`--q` 默认 0）不变：MAE 0.486 / 22655 条 / 2.54 MB。
## v2.6 — 2026-09-26

**三条路线定稿；把「生图」和「草图矢量化」补成可执行脚本；图层树去掉颜色族中间层。**

### 定稿的流程（默认路线 3）

```
IR（= 完善后给生图模型的 prompt）
  ↓
生图简报 → 草图 → ★ 矢量草图 → 闸口①
  ↓
成品位图 → 闸口② → 位图 → 矢量 → 三道门禁 → 交付
```

- `IR` 就是"完善后给生图模型的 prompt"，它约束生图质量。
- 草图 / 成品位图**各有一次物理检查**（同一个 `check_sketch.py` 跑两次）。
- 三道门禁首次在 `SKILL.md` 里被**逐条定义**（此前全文只有名字）：
  ① `check_render.py` 渲染静默失败 ② `delivery_gate.py` 风格偏离
  ③ `check_delivery.py` 投稿合规；外加第四个 `audit_composition.py` 局部构图。

### 新增 1：`scripts/gen_figure.py` —— 第 ② 步：生图

- **key 由使用者自备**（环境变量 `DASHSCOPE_API_KEY`），脚本里**不存任何 key**；
  没 key 也能 `--dry-run` 走通全流程自检（**已实测**）。
- **`--ref` 风格参考图**（可多次）—— `qwen-image-*` 走图生图（支持参考图），
  `wanx*` / `wan*` 走纯文生图。
- 产物全部落盘：草图 / 成品位图 + `calls.jsonl`
  （model / seed / size / refs / prompt 指纹 / 输出文件）。

### 新增 2：`scripts/sketch_to_vector.py` —— 草图矢量化（人可改）

不用写 `panels.py`：白底 + 自动切分，每个形体一个子层。实测
（`assets/demos/sketch_upc.png`）：

```
MAE 0.019 | >8 0.00% | >32 0.00% | PSNR 63.50 dB
<path> 2235 | <text> 0 | <image> 0 | 0.59 MB | 29 个自动 part
图层树：figure → p(panel) → p-part-01(element) → p-part-01-01(<path>)
```

### 改：`raster_vector/groupvec.py` 去掉「颜色族」中间层

原来图层树是 `panel → element → 颜色族 → <path>`。中间那层对"改物理内容"没用
（人不会想"把橙色都改掉"，只会想"把光子线改掉"），而且和"不要按颜色分层"直接冲突。
现在图层树是 `panel → element → <path>`；同色矩形仍合并成一条 `<path>`
（体积优化，保留），但不再为它插一层 `<g>`。`einfo` 的 `families` 字段改名 `colors`，
`<path id>` 从 `p-photon-A-orange1` 改成 `p-photon-A-01`。

实测对照（UPC，`W=1664 R=10 K=7`，同一张源位图、HEAD 版 groupvec vs 改后）：

| | HEAD（三层树） | 改后（两层树） |
|---|---|---|
| MAE / PSNR | 0.486 / 31.44 dB | **0.486 / 31.44 dB**（不变） |
| `<path>` | 22655 | 22655 |
| `data-family` 中间层 | 95 个 | **0** |
| 体积 | 2.66 MB | **2.54 MB** |

回归：`sketch_to_vector.py` 跑 `sketch_upc.png` 仍是 MAE 0.019 / PSNR 63.50 dB / 2235 条。

### 修：「临摹」段落里的三处自相矛盾 / 作废描述

1. `SKILL.md` 旧标题写着「③ 必须是混合临摹」，与同文件「重画（首选）vs 混合临摹
   （备用）」直接打架，而 `ir_to_genbrief` 的另一处又说"看懂后重画、不要照抄位图几何"
   —— 三句互相矛盾，模型读到哪句走哪句。已统一为：**位图过了闸口就该照它画**；
   对不上 = 位图错了，回去重出。旧标题改成「纯像素描摹一定不行：文字会全变成轮廓」
   （保留了原来那条实测依据）。
2. 图层描述里的「面板 / 元素 / 颜色族三层树」已按上面的改动同步为两层。
3. `--manifest` 的默认文件名硬编码成 `t301_grouped.json`（T3-01 调试留下的），
   于是跑任何别的图都会在 cwd 里多出一个叫 T3-01 的 json。现在默认跟 `-o` 同名
   （`<out>_layers.json`）。

### 同步

- `README.md`：横幅、完整流程图、三条路线表、位图 → 矢量表、脚本清单全部对齐 `SKILL.md`。
- `SKILL.md`：新增「三道门禁是谁、各拦什么」「生图那一步的三条硬规矩」
  （key 自备 / 必须给参考图 / 两个产物都落盘）。
- `scripts/check_sketch.py`：说明它在新流程里跑**两次**（闸口① 草图、闸口② 成品位图）。

### 未验证（说清楚，别当已完成）

- **`gen_figure.py` 的联网路径没有实测** —— 手上的 DashScope key 已失效
  （`401 InvalidApiKey`）。目前只有 `--dry-run` 有实测。
- **两次生图的可复现性仍未验证**：同 prompt 同 seed 两次输出是否一致。
  这是路线 3 的死穴，也是它现在只能"出稿"、不能"交付"的原因。
## v2.5 — 2026-09-26

**跑通「生图 → 语义临摹」的完整算例（UPC 示意图），并修掉两个会误判临摹稿的门禁假阳性。**

试样：超边缘碰撞侧视示意图（两个洛伦兹压扁的核 + 两条平行虚线束流轴 + 碰撞参数 b +
γγ 顶点 + e⁺e⁻）。位图由生图模型出（1664×928），再用
`raster_to_vector_semantic.py` 临摹回矢量。可复现算例在
`assets/demos/upc_semantic/`。

### 实测（W=1664, R=10, K=7）

```
MAE 0.486 | >8 0.31% | >32 0.23% | PSNR 31.44 dB
<path> 22655 | <text> 10 | <image> 0
check_delivery  : ✅ 矢量对象 22656 | 嵌入位图 0 | 最小字号 8.7 pt
audit_composition: ① 0 ② 0 ③ 0 ④ 0/9 → ✅
```

### 修复 1：`elements.py` 元素吞掉外接矩形内的背景

`_split_comp()` 用「最近种子」铺满时，`distance_transform` 是对**整个 crop**
求最近种子，而 `mm[y0:y1, x0:x1] = sub == i` 没有限制在连通域内 ——
于是每个元素都把自己外接矩形里的背景整块吃下来。

| 元素 | 修前 px（背景占比） | 修后 px（背景占比） |
|---|---|---|
| `photon-A` | 83414（97.7%） | **2305**（30.2%） |
| `photon-B` | 102775（97.3%） | **3329**（30.3%） |
| `field-A` | 17954（89.9%） | **6973**（40.9%） |
| `arrow-b` | 10072（84.9%） | **1861**（18.4%） |

后果：选中 `photon-A` 图层会连带选中一大块白底，「点选物理元素」直接失效。
修法是一行：`mm[y0:y1, x0:x1] = (sub == i) & m`。
**MAE 完全不变**（四叉树矩形本来就是不重叠的，命名不影响像素）。
回归：T3-01（W=1200, R=16, K=7）修前 MAE 0.293 / 43057 条 `<path>` / 50 个元素，
修后 MAE 0.293 / 43024 / 49 —— 保真度无变，仅元素边界收紧。

### 修复 2：两道门禁会把位图临摹稿误判阻断

**(a) `check_delivery.py` 把渐变当成嵌入位图。**
MuPDF 的 `page.get_image_info()` 会把**渐变填充（shading pattern）**也合成成一条
“图像”记录（特征：xref=0、像素尺寸正好等于该图元 bbox）。
最小复现：只含 1 个 radialGradient + 1 个 linearGradient 的 SVG 走 Edge
print-to-pdf 后 `get_images()=0`、原始字节里 `/Subtype /Image` 出现 0 次（只有 `/Shading`），
但 `get_image_info()` 报 **2 张“57×64 px”的位图** → 旧版直接判
“嵌入 2 个位图，72 dpi”。改为 `get_image_info(xrefs=True)` + 扫 xref 找真
`/Subtype /Image` 后，figA 报“另有 71 条伪位图已剔除”，figB 15 条，两者现在全绿。
反向对照：真塞一张 240×160 PNG → `get_images()=1`、xref 扫到 4，仍被如实报告。

**(b) `audit_composition.py` 把页面白底当成「出界图形」。**
位图临摹稿的页面就是图本身，所以白底矩形总是顶到边；实测 upc.pdf 被 ③
报 3 处出界，但 fill 全是 `(1,1,1)`/`(1,0.996,0.996)`/`(1,1,0.996)`，y1 仅超出
0.54pt（Edge 打印的像素对齐），且同时 ① 文字重叠 0 处、② 线穿文字 0 处。
保真度不会因为它被裁而受损（白上白不可见）。现在：几乎盖满整页的近白色
填充不进入 ③/④ 的判定，并在输出里如实报出被剔除条数。
回归：figA / figB / figC 三张旧产物的 ③ 结果均不变（figA、figB ✅；figC 仍为
它自己的 6 处线穿文字，与本次修改无关）。

### 另记：位图临摹稿与直写 SVG 稿的对比

同一张 UPC，三条路线并排见 `assets/demos/upc_semantic/cmp_preview.png`。
上一轮直写的 `figures/out/figC_upc.pdf` 过不了 `audit_composition`
（② 线穿文字 6 处，0.15–0.28%），而位图临摹稿四项全过——临摹稿的
文字位置是从位图里量出来的，不会像手写那样自己造出重叠。

## v2.4 — 2026-09-22

**端到端跑通「草图 → Nature 级配图 → 临摹回矢量」这条主线，顺手抓出 4 个真 bug。**

试样：一张 4 格手绘草图（形变核 → 量子涨落 → 碰撞 → QGP 火球）。先用
`cartoon_lib` 直出矢量版，渲染成 2160×680 位图（= 「Nature 级位图」），
再用 `raster_to_vector_semantic.py` 把它临摹回 SVG，两条产物逐像素比。

### 实测（1080 / 2160 两个工作分辨率）

| | 直出矢量 | 临摹 W=1080 | 临摹 W=2160 |
|---|---|---|---|
| 体积 | 14.7 KB | 1.38 MB | 2.43 MB |
| `<path>` | 12 | 12063 | 18638 |
| `<text>` | **7** | 6 | 6 |
| `<image>` | 0 | 0 | 0 |
| MAE / PSNR（同分辨率回渲染） | — | **0.738** / 30.5 dB | **0.230** / 36.1 dB |

结论：**这类「平色块 + 硬边」的示意图，临摹的像素保真度相当好**（1080 下 MAE 0.74），
但代价是三条硬成本 ——

1. **文字可编辑性会掉**：图注（单行 718 工作像素宽）过不了词表 `w > 400` 的闸门，
   只能留在色块里 → 直出 7 条 `<text>`，临摹只剩 6 条。
2. **分辨率天花板 = 工作分辨率**：2160 下 MAE 从 0.74 掉到 0.23，
   但体积从 1.4 MB 涨到 2.4 MB、路径涨 55%。想再高就得再翻倍。
   （直出矢量在任意缩放下都是精确的。）
3. **每张图要手写两张表**（`words.txt` + `panels.py`），且**表必须跟着工作分辨率走** ——
   1080 的表配 2160 的画布会静默出错（见 bug 3）。

### 本轮修的真 bug（都由这次端到端对照暴露）

1. **`fonts.pick()` 环境变量分支猜字体族名** —— 原来写
   `"Arial" if "arial" in p.lower() else "Liberation Sans"`，于是
   `HEP_VEC_FONT=DejaVuSans.ttf` 会在 SVG 里写 `font-family="Liberation Sans"`，
   而量字宽用的是 DejaVuSans.ttf。两者字宽不同 → **文字整体错位，且完全静默**。
   修：新增 `_family_of()`，用 fontTools 从字体文件本身读 family name（本包已有该依赖）。
   这条正是本模块头号纪律「量字宽的字体必须和 SVG 里的族名一致」的违例。
2. **同一条标签同时出现在词表和 `MANUAL` 里 → 同一句话画两遍**。
   人工修 OCR 错字时把词条挪进 `MANUAL` 是最自然的做法，两个候选都会通过逐词自校验，
   肉眼就是 `Eollision` 这种重影。修：候选去重（与 MANUAL 框 IoU > 0.35 的词表候选丢弃）。
   实测 MAE 1.276 → 0.738，PSNR 25.1 → 30.5 dB。
3. **`cell_of()` 返回 -1 时 `P.CELLS[-1]` 绕到最后一个面板**。
   文字被静默塞进错误的图层，而 id 看起来完全合法（`d-t1`）。
   实测暴露：工作分辨率 1080 → 2160、表没跟着改时，6 条标签全被塞进 `d`。
   修：新增 `cid_of()`，越界返回 `None` 而不是负索引。
4. **`dh > 2` 是绝对像素闸门，不随字号缩放** → 高分辨率下**误杀拟合更好的标签**。
   实测 "Quantum" / "QGP fireball" 在 2160 下 res0 = 0.009（闸门 0.14，好一个数量级），
   却因为 dh=4 > 2 被丢掉、退回成色块轮廓。修：`labels.dh_lim()` 按墨迹高取比例
   （`max(2.0, 0.18·h_ink)`）—— 1080 下仍是 2.0，与调好的行为一致。

5. **`svg_lib._detect_cjk_font()` 用本地编码解 `fc-list` 的 UTF-8 输出** ——
   中文 Windows 上每次 import / `render()` 都往 stderr 刷一屏 `UnicodeDecodeError`
   traceback（实测 fc-list 存在时会走到这条路），而代码里明明有 `except` 兜底，
   用户看到红字只会以为坏了。修：显式 `encoding="utf-8", errors="replace"`。

**回归**：T3-01 与 QGP 两张已验收的图重跑，数字逐项不变
（QGP：MAE 2.753 / `<text>` 21 / `<path>` 21009 / 3.27 MB），零回归。

### 建议

**能用矢量直出就别绕位图。** 「生图 → 临摹」是给**只有位图可拿**的场景兜底的
（扩散模型出图、Blender 渲染、别人给的截图）。既然生成端是可控的，
直接产 SVG 才是无损的：文字 7/7 可编辑、任意缩放精确、14.7 KB vs 1.4 MB。

## v2.3 — 2026-09-22

**斜排标签（斜着排的箭头说明）纳入「擦掉重写为真 `<text>`」；位图→矢量在第二张图上验收。**

第二张图是 3D 圆柱 + 火球的 QGP 初态全流程示意图（1200×865，7 面板 / 23 物理元素，
大量柔和渐变 + 细网格线），比 T3-01 难得多，正好用来压这套流水线的边界。

### 实测（QGP 朝向涨落示意图，1200×865）

| 指标 | 值 |
|---|---|
| 非文字区（纯矢量色块）MAE / >32 | **1.23** / 0.59% |
| 整图 MAE / PSNR | 2.75 / 22.2 dB |
| `<path>` / `<text>` / `<image>` | 21009 / **21** / **0** |
| 文字覆盖像素 | 1.77%，但占整图误差 56% —— 真 `<text>` 是按 Arial 重排的，**亚像素级不可能逐像素重合**，这块误差是「可编辑性」的代价，不是缺陷 |
| 体积 | 3.3 MB（R=16）；R=12 → 3.6 MB、MAE 2.65（+10% 体积换 −3.7% 误差，不划算） |

### 新增

- **斜排标签自动拟合** —— `labels.align_rot / derotate / _text_band / rot_box_fit / rot_mask`。
  做法是「把图绕框心**转正**，再交给已验证的水平 `align()` 三段式」：
  - ① 角度**按实际墨迹残差**挑（`score = res0 + 0.004·dh`），不用几何启发式 ——
    实测「行带最矮」会把 43° 带偏到 50°。
  - ② `_text_band()` 按「滑窗内**总墨迹**」挑文字行带，把同角度的长直线（箭头 / 板边）排掉。
  - ③ 验收门槛比水平文字更严（`res0 ≤ max(0.14, 0.55·ink)` 且 `dh ≤ 5`）；
    **拟合不过关的整条不擦、原样保留成色块**（宁可不改成 `<text>`，也不能把原来对的东西擦坏）。
- **数学符号兜底字体** `fonts.pick_math()` —— 主字体 Arial 缺 `⊥`(U+22A5) / `≳` / `⟨⟩`，
  缺字的标签会被静默丢掉。候选顺序 `HEP_VEC_FONT_MATH` → `DejaVuSans.ttf` → `ARIALUNI.TTF`
  → `seguisym.ttf`（实测 DejaVu Sans 全有）。找不到就照旧丢标签，不报错。
- `labels.set_fam / choose_fam / can_render` —— 逐字符判有无字形，缺字时**整条**换兜底字体。

### 改

- `labels._hm` 字体度量缓存**按字体文件分开**（主字体和兜底字体的 hmtx 不能混用）。
- `groupvec.emit()` 的文字层从「各面板内部」挪到所有面板之后的统一 `<g id="text-layer">` ——
  原来后面面板的不透明背景色块会盖住前面面板的文字尾巴（实测 `Pressure-driven` 在 x=950
  被切断、`Low-energy method` 在 x=320 被切断）。

### 本轮修的真 bug（都有实测依据）

1. **`--panels` 只替换了 `groupvec` 的 `P`**，`elements.py` 内部自己 `from . import panels as P`
   仍用包内默认表 → 换图必崩 `KeyError`。修：`assign_elements` 里 `ELC.P = P`。
2. **`read_words` 的 `w*h > 3000` 面积上限**把长正文行整条丢掉
   （`Pressure-driven hydrodynamic expansion` 这类）→ 只保留单边尺寸过滤。词表 14 → 22 条。
3. **`emit()` 硬取 `meta["title"]`** → 没有总标题的图必崩。改成
   `(meta.get("title") or {}).get("desc") or basename(out)`。
4. **`--check` 在诊断子命令下**（`--elmap/--npz/--segs`，本来就不出 SVG）仍去回渲染自检 → 报错。
   修：加 `if not Path(a.out).exists()` 分支，提示「去掉它们再自检」。
5. **`ocr_words.ps1`**：UTF-8 **无 BOM** 被 PowerShell 5.1 读成乱码 → 改 UTF-8 **with BOM** + CRLF；
   `New-Object Bitmap($w*2,$h*2)` 在 PS 5.1 上报 `op_Multiply` → 改 `-ArgumentList`。
6. **斜排标签擦除用了轴对齐包围盒** → 两条平行标签的 AABB 必然互相重叠，把旁边那条擦掉半截
   （实测留下 `"on"` 碎片、另一条被擦穿导致文字画了两遍）。
   修：`labels.rot_mask` 改用**旋转后的四边形**掩码，只擦自己那条。
7. **斜排标签只擦「重写文字」的框** → 拟合文字比原字略窄时，原字右侧笔画整段留下
   （实测 `Low-energy method` 右端留下一个像 `!` 的残影）。
   修：`align()` 的 meta 增加 `irect`（**原图**墨迹框），擦除改用 `rect ∪ irect`。
8. **`rot_el` 的退路用裸字符串** → `Large ε_{2} small d_{⊥}` 会把 `_{2}` 字面画出来。
   修：退路也走 `text_el`（`<tspan>` 上下标）。

### 已知边界

- **细网格线 / 网纹**：1px 暗线压在浅渐变上，四叉树按块均值取样会把线**压淡**，
  非文字区那 0.59% 的 >32 像素基本都出在这里。要根治得再加一层「暗线描边」通道，
  目前没有；把 R 从 16 压到 12 也救不回来（实测误差只降 3.7%，体积涨 10%）。
- **原图本身被画布裁掉的文字**：源图里 `Prolate-deformed nucleus` 左端被裁（显示成
  `plate-deformed nucleus`），OCR 词表给的是完整词，重排后是完整的。
  这是**有意保留完整词**（投稿图不该有断词），不是拟合失败。

## v2.2 — 2026-09-22

**位图 → 矢量补上第二种粒度：`raster_to_vector_semantic.py`（先理解再临摹）。**

v2.0 定的架构是「临摹管矢量化」，主路径是 `raster_to_vector.py`
（逐像素描摹 + 文字擦掉重写 + `--groups` 语义归组）—— **那条原样保留**。
本条补的是**另一种粒度**：适合「拆不成图元、但颜色是平的」渲染示意图。

| | `raster_to_vector.py`（逐像素描摹） | **`raster_to_vector_semantic.py`（先理解再临摹）** |
|---|---|---|
| 文字 | 模型写 `--text-spec`，擦掉重写 | **OCR 词表 + 逐词对齐**（字号/字距/位置自动估） |
| 图层 | 归组靠 `--groups`（模型给代表点） | 归组靠 `panels.py` 的 **ELEMENTS 表**（物理元素命名） |
| 误差 | 可选 | **自带 MAE / PSNR / `--check` 回渲染** |
| 依赖 | cv2 / skimage | numpy / scipy / Pillow / cairosvg / cairocffi / fontTools |

两条**不冲突**：描摹那条按"颜色"忠实压平；这条按"物理元素"建图层树，
**人能直接点选某个物理元素改它**。要前者要忠实度，要后者要可编辑性。

### 实测（T3-01 Jia2026 Fig.1，1200×1133）

| 指标 | 值 |
|---|---|
| 非文字区（纯矢量色块）MAE / >32 色阶像素 | **0.227** / **0.00%** |
| 整图 MAE / PSNR | 1.48 / 26.5 dB |
| `<path>` / `<text>` / `<image>` | 37930 / 54 / **0** |
| 图层 | 12 面板 / **50 物理元素** / 三层树（panel → element → color family） |
| 体积 | 4.7 MB（R=16）；R=32 → 4.1 MB，MAE 1.65 |

同一份输入**三次独立运行 → SVG 逐字节相同**（md5 `6bf4470a…`）。

> 验收教训：旧版描摹的验收用 **PIL 大字降采样**当代理，**系统性偏乐观**；
> 换成与成品相同的渲染器（cairo）后，单词级实测 3/4 更优。
> **验收必须用与成品相同的渲染器。**

### 新增

- `scripts/raster_to_vector_semantic.py` —— 新 CLI
  （`--words / --panels / --W / --R / --K / --no-text / --no-erase / --legend /
  --manifest / --elmap / --check`）
- `scripts/raster_vector/` —— 库：`quadtree` `labels` `elements` `segsem`
  `panels`（示例）`groupvec` `raster_ops` `fonts` + `ocr_words.ps1`

### 改

- `check_delivery.py` —— 位图门禁从「软提示」改成**阻断**：
  - 位图只能出现在**受控区域**（单张占页 ≥90% = 整页被栅格化 → ❌）
  - **有效 dpi 实算**（像素数 ÷ 版面物理尺寸，不看标称 dpi）：<300 ❌ / <600 ⚠️
  - 位图之外**必须有矢量轮廓**（顶层指令 <5 且无 XObject → ❌）
  - 实测 4 个用例：整页位图 ❌ / 受控区域高清位图 ✅ / 低 dpi 大位图 ❌ / 真交付件 ✅
- `SKILL.md` / `README.md` —— 补 `raster_to_vector_semantic.py` 这一支；脚本表、目录结构同步
- `requirements.txt` —— 补 `cairocffi` / `fonttools`

### 本轮修的三个真 bug（都有实测依据）

1. **面板框重叠 14px** → 竖带矩形重复出图两遍（32132 → 37932 条）。
   修：`h2 (0,386,456,430)`、`h3 (0,770,456,816)`。回归：88671 矩形、重复 **0**。
2. **颜色合并链式反应**把浅色圆柱并进白底 → 改用**墨迹连通域**定形状。
3. **元素命名全部命中 `background`** → 改为「自动切分定形状 + 手写表按 IoU 命名」。

### 已知边界（别踩）

- **渐变**是色阶台阶，**不是** vector gradient mesh。要更平滑只能调小 `--R`（路径数涨）。
- **公式**（⟨V₂(η₁)V₂*(η₂)⟩ 这类）字体缺衬线字形，残差天然偏大。
- **只适合「色块 + 硬边」类图**。照片、有机纹理不适合。
- 三条工程约定：矩形只能"列相邻且同色"才合并；矩形按面板边界切开；
  `font-family` **只写单值**（列表 cairo 解析不了，静默退回 sans-serif，宽度差 11.5%）。

## v2.1 — 2026-09-21

两件事：**语义归组**（安全版）+ **参考图 vs 画法规律的优先级**。

### ① `raster_to_vector.py --groups`：语义归组

**问题**：描摹出来是**按颜色**分层的（`00 #cccccd`…），没有语义 ——
同色不同物被并进一层，一个物体跨色被拆到几层。

**解法**（和文字清单同构：**模型看懂 → 工具按标注执行**）：模型给每个语义对象
一个代表点，工具按"点在哪个多边形内（**最内层优先**）"定归属。

```yaml
groups:
  - {name: "QGP 介质", at: [0.45, 0.50]}
  - {name: "入射核",   at: [[0.36,0.50], [0.55,0.50]]}
```

**★ 关键设计：归组不改绘制顺序。** 描摹是"按颜色覆盖率从大到小"画的，
这个顺序决定遮挡。重排层序会让底下的跑到上面 → 图就变了。

**实测**：归组版 vs 不归组版，**最大像素差 = 0，逐像素相同**。

### ★ 踩了两个坑（都是我自己造的）

1. **逐多边形打标 → 洞没了。** 第一版把每个多边形拆成独立 `<path>` 好挂
   `data-group`，结果**最大像素差 255**。根因：同色多边形合并时靠
   `fill-rule="evenodd"` **互相抵消产生洞**（环形就是这样描出来的），
   拆开各自填充 → 洞消失。
   → **必须保持合并**；归属信息改走旁路 `<metadata>` 索引。

2. **合并后 `data-group` 只能挂一个** —— 等于没归组。
   这是上一条的起因，也是它被否掉的原因。

### ② 参考图 vs 画法规律：**能直接给图就别提炼**

用户问破了：那些"规律"（核带网格线、火球有纹理、衬板给中间调…）
**就是从 8 张 T3 精选看图看出来的 —— 是总结，不是源头。**

| | 给参考图 | 给提炼的规律 |
|---|---|---|
| 效果 | **无损、最好** | 有损压缩 |
| 内部/本地 | ✅ **直接用图** | 不需要 |
| **公开发布** | ❌ 版权 | ✅ 只剩这条路 |

**「风格档案（数字）」和「画法规律（文字）」本质是同一个东西：
参考图的【版权干净代理物】。是降级方案，不是方法论。**

**顺带修了文档 bug**：`SKILL.md` 里写着 `assets/t3-exemplars/` 有"8 张"，
实际仓库只有 **2 张**（CC-BY）—— 早期改许可时漏改的。

---

## v2.0 — 2026-09-21 架构定稿：IR 管物理 / 生图管风格 / 临摹管矢量化

用户否掉了两处**我制造的复杂度**，都是对的：

### ① "Scene Graph" 是冗余的 —— 它就是可执行 IR

把格式摆出来看，**同一个东西**：

```
IR:            figure / elements(primitive+params+physics_role+z) / geometry_constraints
"Scene Graph": layers / elements(primitive+params+physics_role+z+layer) / text / formula
```

只多了 `layers`/`text`/`formula` 三个字段。**它"保证物理正确"的活本来就是 IR 干的**，
包装成新的一层是噪声。

→ `scene_render.py` 保留，但**重新定位为「IR 直接渲染成 SVG」的可选后端**，
   只在"需要逐字节可复现 / 批量参数扫描"时用。**自由创作和复现都用不到它。**

### ② 自由创作不需要图元库 —— 生图 + 逐元素临摹即可

用户的判断：让位图符合 Nature 风格 → 模型逐元素临摹成 SVG。
**图元库是有损参数化，而模型直接画天花板更高。**

→ 图元库缩为**确定性工具**（改一个数重出、批量扫描、模型绘画能力不足时兜底），
   不再是"画图"的主力。

### ③ 新增：混合临摹（这是补上的实质能力）

**为什么必须混合**：Nature 硬性要求**文字可编辑**。
纯临摹的文字是**轮廓路径** → `check_delivery` 判"文字不可提取" = 不合规。

```
① 模型看图 → 标出「哪些区域是文字」+「写的是什么」
② 工具：文字区域先用周围底色擦掉，其余逐像素临摹
③ 文字用真 <text> 写回
```

**实测对比**（同一张图）：

| | 纯像素描摹 | **混合** |
|---|---|---|
| `<text>` 元素 | **0 个** | **3 个** |

```bash
python3 scripts/raster_to_vector.py fig.png -o fig.svg --text-spec texts.yaml
```

### ④ 钉死一条：物理检查要在【最终矢量】上做

**位图对 ≠ 重画出来的对。** 实测（`sketch2nature_demo/`）：同一模型从位图重画时，
几何约束 **6 项只过 2 项**（喷注夹角 173° 应 180°、表面偏置 0.85 应 0.72）。

所以 `geometry_constraints` 得在重画之后再过一遍。

---

## v1.9 — 2026-09-21

**新方向：位图 → 矢量走「Scene Graph + 确定性重组」。**

### 用户点出的关键

图像模型能从草图画出不错的位图，但**"位图 → 矢量"这一步它做不好**。
试过的两条路都不行：

| 路 | 为什么不行 |
|---|---|
| 像素描摹（`raster_to_vector.py`） | **没有理解**：文字碎成色块、渐变退化成色阶台阶 |
| 让模型看着位图重画 | **有理解但不忠实、不确定**：漂移、自己发明、两次不一样 |

**第三条路**：
```
看懂这张图 → 建 Scene Graph → 提取对象/文字/公式/几何/层级/坐标 → 确定性后端重组
  模型           模型                        结构化                    scene_render.py
```
理解力来自模型，**忠实度与可复现性来自"重组是代码"**。

### 新增 `scripts/scene_render.py`

Scene Graph → SVG 的确定性重组后端。**已实测：同一份 Scene Graph
两次重组，SVG 逐字节相同**（md5 一致）。

格式在可执行 IR 上扩了三样：
- **`layers`** —— 有名字的组（层级是显式的，不再靠 z 推）
- **`text`** —— 文字是独立类型
- **`formula`** —— 公式用 Unicode 直写，保持可编辑

校验查：primitive 认不认识、参数名在不在签名里、**必填参数漏没漏**、
`layer` 在不在 `layers` 里、id 是否重复、有无 `physics_role`。

### ★ 可复现性的定义被澄清

用户明确：**不是"相同 prompt 必须输出一样"**，
而是「**对 skill 库里已有的图片保持极高的可复现性**」。
`scene_render.py` 的"同一 Scene Graph → 逐字节相同"正是这个要求。

### 没做完的（接手就做）

1. **真正的提取流程一次都没跑过** —— 拿真实位图让模型提取成 Scene Graph，
   再重组、对比原图。这是验证这条路成不成立的关键实验。
2. **复杂矢量图仍不好看** —— Scene Graph 只解决"忠实 + 可复现"，
   不解决"美学上限"。别又变成"以为能自动达到 Nature 级"。
3. **缺"怎么从图上看出来"的提取规范** —— 现在只有格式，没有方法。

### 同时完成：装箱前修复 + 三个版本

见下一条。

---

## v1.9b — 2026-09-21 装箱前修复 + 三版本上传

**从 GitHub 装之前该修的 5 项全部修掉：**

| # | 问题 | 修法 |
|---|---|---|
| ① | **GitHub 停在 v1.0**（本地领先 4 提交从未 push） | 已推：4 分支 + tag |
| ② | README 没反映新分工（grep 命中 0） | 重写：加分工 + 外部 A/B 证据 + 路线选型表 |
| ③ | `requirements.txt` 缺 3 个实际 import 的依赖 | 补 PyYAML / opencv-python / scikit-image |
| ④ | 「走哪条路」埋在 SKILL.md 第 330 行 | 提到第 32 行 |
| ⑤ | `check_delivery --help` 被当文件名 | 改 argparse，**顺带支持 `--svg` 查 SVG 合法性** |

**三个版本**（用户要求，最新是 B2）：

| 分支 | 路线 | 脚本数 |
|---|---|---|
| **`main`** | **B2（最新）** | 24 |
| `route-a` | A：模型直接写 SVG | 21 |
| `route-b1` | B1：生图当草图 | 23 |
| `route-b2` | = main | 24 |

每个分支回归 **13/13 全过**，无脚本 import 被删模块。

**建分支时踩的坑**：`git rm ... && git commit` —— `git commit` 不带 `-a`
**只提交已暂存的**，于是 `git rm` 的删除进去了、README 改动没进去，
后来被 `git add -A` 扫进了 `main`（main 上挂了 A 版本的横幅）。
回滚到干净基线重做了一遍。**教训：混用 `git rm` 和普通 `commit` 时必须 `git add -A`。**

---

## v1.8 — 2026-09-21

拿**外部实跑产物**（用户用 GPT 跑的 `sketch2nature_demo/`）验证了门禁，
**抓到两个它自查没发现的真缺陷**，并修掉我自己工具的一个崩溃。

### 抓到什么

| # | 缺陷 | 为什么它自查发现不了 |
|---|---|---|
| 1 | 底部图注 **跑出左边界**（会被裁） | 它是"看着好好的"——要量 PDF 几何才知道 |
| 2 | SVG 根元素 **`viewBox` 写了两遍** | **浏览器宽容能渲染**，但 Illustrator / cairosvg 直接拒绝 → 会挡投稿 |

第 2 条特别值得记：**"浏览器能开"不等于"专业工具能开"**，
而 Nature 要求图能在 Illustrator 里编辑。这类缺陷只有严格解析器才发现。

### 修了什么

- **`repair_brief.py` 之前会崩**：遇到非法 SVG 时 cairosvg 抛
  `duplicate attribute`，脚本整个挂掉。现在：
  - 新增 `check_svg_xml()`：查 XML 合法性，**报出重复属性的元素名与行号**
  - 解析失败**不再崩**，而是当成一条"必须修"报出去，
    并如实说明"几何检查做不了"
  - 同一根因（重复属性 → 解析失败）不报两条

### 验证：外部实跑的 A/B 很说明问题

用户用 GPT 跑的 `sketch2nature_demo/` 里有一组对照：

| | solo 版（不调 skill） | skill 版（受 IR 约束） |
|---|---|---|
| 观感 | 一样漂亮 | 一样漂亮 |
| 几何约束 6 项 | **过 2 项** | **过 6 项** |

solo 版的具体违规：装饰色环+光点（IR 禁止）、escaping jet 出界、
胶子辐射两条喷注都有（应只有淬火那条）、两喷注夹角 **173°**（应 180°）、
**|v−C|/R = 0.85**（应有 0.72 —— 表面偏置丢了）。

**这正是整个项目的论点**：图像模型单干能画出漂亮但物理错的图；
约束加上去，观感不变而物理全对。

### 门禁对它产出的判定

- 构图审计：🚫 1 处（图注出界）—— **GPT 自己没发现**
- 交付合规：✅ 通过（矢量、433 字符可提取、最小字号 8.6pt）
- 风格门禁：✅ 通过（0 阻断；`dark_ratio` 偏离 88% 但类内分散，只提醒）
- 返修单：🚫 1 处必须修（重复 viewBox）

---

## v1.7 — 2026-09-20

路线 B 拆成两条子路径，并补上**中间稿的检查闸口**。

### 用户的方案（比我原来的好）

原来的 `ir_to_genbrief` 让图像模型**直接出成品** —— 隐患是：成品一出来，
下一步"绘画"就变成**照抄**，位图里的物理错误会被一起抄进矢量。

用户的方案把产物定位成「**更好的草图**」—— 是**照着画的依据**，不是要复制的成品：

```
路径 1  草图/描述 ──生图──▶【更好的草图】──绘画──▶ 矢量
路径 2  路径 1 + 检查闸口 + 成品位图
```

**物理约束因此留在了正确的位置**：绘画那步仍要按 IR 正确执行。

### 新增 `scripts/check_sketch.py`（路径 2 的闸口）

**机器判不了物理** —— 图像模型可能把喷注画反、把非中心碰撞画成同心、
把 L 画成面内箭头，这些从像素上量不出来。所以输出分两块：

| 输出 | 谁判 |
|---|---|
| ■ 自动测到的：内容边界/出界、重心偏移、留白分布、**画布比例被改**、风格量测 | 机器 |
| ■ 必须逐条回答的：IR 的 `geometry_constraints` 变成待答问题 | **人/模型看图** |

把"检查物理"从**一句原则**变成**必须填的动作**，才拦得住错。
结论会明说："自动检查通过，但上面那一串问题还没答，逐条答完才能往下走。"

### `ir_to_genbrief.py` 加 `--stage`

- `--stage sketch`（默认）：只求构图清晰、元素齐全，**明说不用管质感**
- `--stage render`：要质感/光影/风格，配风格档案

### 诚实标注：路径 2 的第二次生图没验证过

每次生图非确定，两次就是两次漂移机会；第二次是"看着第一次的图再画"，
可能二次解释、进一步偏离。**文档里标了"先用它跑一张对比再决定要不要固化"。**

### ChatGPT 版

Instructions 补了路线 B 的用法（ChatGPT 里图像生成可用，这是它最该知道的路），
含两条铁律：产物是草图不是成品 / 临摹是看懂再重画不是像素描摹。
`ir_to_genbrief.py` 与 `check_sketch.py` 已进流程捆。

---

## v1.6 — 2026-09-20

新增**路线 B：图像生成 → 临摹成矢量**，并补齐它的第一环。

### 背景：一个被实测推翻的做法

用户问"为什么 GPT 把位图转矢量比你的好"。查下来是**方法差别**：

| | 像素描摹脚本（我写的） | 模型看图重画 |
|---|---|---|
| 原理 | 等高线 → 填色路径，**没有"理解"** | 看懂"这是圆/字/渐变"再写 |
| 文字 | **碎成色块** | 真 `<text>` |
| 渐变 | **退化成色阶台阶** | 真 `<gradient>` |
| 图层 | 只能按**颜色**分 | 按**语义**分 |

实测：`raster_to_vector.py` 拿 T3-05 跑，结构保住了（面板/坐标轴/箭头/场都在），
但"B"变成一个填色块、坐标数字变形。**这不是调参能补的。**

→ `raster_to_vector.py` 降级为**兜底**（模型不可用时），**不是主路径**。
→ 临摹那一步必须交给模型做。

### 顺带实测到的一条

**色阶数不是越多越好。** 我原以为调高更接近原图：

| 色阶 | 路径数 | 观感 |
|---|---|---|
| 12 | 504 | 能认，结构全在 |
| 24 | 601 | — |
| 48 | 671 | **更差** —— 色带变成一圈圈等高线伪影 |

色阶多了每级变薄，路径简化就产生可见锯齿环。12 是甜点。

### 新增 `scripts/ir_to_genbrief.py`

把 IR 变成**给图像生成模型的约束简报**（路线 B 的第①环）：

- **物理内容**：要出现的物理对象 + 数量（按名族合并，"Λ 自旋 ×6" 一条）
- **文字清单**：单独一栏，拼写数值必须对
- **构图**：布局 + 各元素归一化位置 + 叠放顺序
- **★ 几何约束单独一节**：标注"图像模型不知道物理，这是最容易画错的地方；
  临摹时必须逐条对账，**不能照抄位图几何**"
- **风格规则**：矢量插画风 / 深色描边 / **先定全局光源**
- **明确禁止**：加 IR 没有的元素、画错文字、改视角、画成照片级

**踩到的坑**：第一版直接把 IR 的 `elements` 倒出来 —— 36 条，
全是实现细节（每个投影、每个自旋记号、每个标签各一条），模型没法用。
**可执行 IR 里是"实现元素"，简报要的是"语义内容"**，两者不能直接映射。
改成按 primitive 分类 + 按名族合并后降到 13 条。

### ★ 必须钉死的一条

**位图的物理不可信。** 图像模型会把喷注画反、把非中心碰撞画成中心碰撞。
**临摹时必须拿 IR 的 `geometry_constraints` 校正，不能对着位图照抄** ——
否则"好看但物理错"会从位图传染到矢量。

### SKILL.md

新增「路线 B」一节，含两条路的选型表（默认走 A：约束更紧、可复现性更好；
要质感走 B，但临摹那步要格外较真）。

---

## v1.5 — 2026-09-20

**分工改了：画图交给模型，本 skill 负责约束、验收、返修。**

### 为什么改

之前一直把 skill 当成"画图的那一方"，于是所有努力都花在
"怎么用代码把图画好看"——**而那正是性价比最低的一条路**。
实测：纯代码管线产出的是"干净的教科书插图"，够不到期刊观感。

模型直接写 SVG 有视觉先验，能做出代码拼不出的质感。
**但模型单干有两个它自己解决不了的短板**：
① 会画出"好看但物理错"的图 ② **它看不见自己的输出**，改不对地方。

这两条正好是 skill 能补的。所以分工应该是：

| 环节 | 谁 | 为什么 |
|---|---|---|
| 画 | 模型 | 有视觉先验 |
| 约束 | IR + geometry_constraints | 防"好看但物理错" |
| 验收 | 三道门禁 | 模型看不见自己的输出 |
| **返修** | **`repair_brief.py`** ← 本次补的 | 光说"文字重叠"模型只能猜 |

### 新增 `scripts/repair_brief.py`

把门禁报告翻译成**给模型的返修单**：

```
■ 必须修（1 处，不修不予交付）
1. 【文字重叠】quenched × Surface —— 重叠 29%
   位置（归一化 x0,y0,x1,y1）= (0.041, 0.903, 0.143, 0.942)
   改法：把「quenched」或「Surface」平移开…建议沿纵向错开至少 22pt
```

原来门禁输出只给人看（"文字重叠: A × B"），模型拿到只能猜往哪挪。

**它比门禁多查一项：行间过近。** 门禁只管重叠，不管"快贴上"——
实测收敛结果里两行文字只隔 **0.9pt**，视觉上已经贴住，门禁判 ✅。
返修单会区分"行内正常词距"（不报）和"行间过近"（给具体下移量）。

### 文档改动

- `SKILL.md` 开头新增「分工：谁画、谁约束、谁验收」一节
- ChatGPT 版 Instructions：第 4 步从"用代码生成"改成"**你来画，直接写 SVG**"，
  并要求**先定一个全局光源**再推导所有光影；第 5 步改成
  "**验证 + 返修闭环**：拿返修单改，直到『必须修』清零"
- `repair_brief.py` 已进 ChatGPT 门禁捆（工具包 30 个文件，自检全绿）

### 仍然悬着的一条

**可复现性**：同 prompt 两次输出是否一致。这是模型直接画这条路的死穴，
也是 `图像测试素材/` 的 T3 测试要回答的问题。**如果不可复现，
这条路只能做初稿，最终交付仍需确定性管线。**

---

## v1.4 — 2026-09-19

`auto_converge` 加**每轮构图门禁**，并修掉两个真 bug。

### 为什么要加（实测证据）

拿 `--ref` 对着 **T3-02**（跨图）跑收敛：loss 从 4.24 降到 2.50，
但优化器把 `label_scale` 顶到上界 1.6 —— **标签压成一团、底部说明跑出画布**。
而 `auto_converge` 当时**完全不知道**，把那轮报成"最好的一轮"；
直到下游的 `audit_composition` 才发现，8 处问题、阻断交付。

**loss 降了，图坏了** —— 这是"指标不是目标"最干净的一次现场演示。

### 改动

- 每轮渲染后跑 `audit_composition`，破坏构图的候选**一律不作废成 best**，
  按"变差"处理（回退 + 步长减半）
- 驱动脚本只出 SVG 时，用 cairosvg 转临时 PDF 再审计（两个驱动都出 SVG，路总是通的）
- 基准参数本身就不过门禁时**立刻停**并说明"这是参数起点的问题，不是搜索能修的"
- `--no-composition-gate` 可关；汇总报告否掉了几个候选

### 效果对比（同一个实验，同一份参数空间）

| | 无门禁 | 有门禁 |
|---|---|---|
| 最终 loss | 2.50 | 3.46 |
| `label_scale` | 1.60（撞界） | 1.12 |
| 构图审计 | 🚫 8 处，阻断 | ✅ 通过 |

**主动用更高的 loss 换一张能交付的图** —— 这是正确的取舍。

### 顺带修的真 bug

1. `render()` 把 cwd 切到脚本目录，却用**相对路径**调用脚本
   → 换调用方式就报 "No such file"。改为先 `resolve()`。
2. 基准就坏构图时，`best` 仍是 None，参数不变 → 下一轮渲出一模一样的东西、
   被同一理由再否一次，**空转到 max_iter**。改为立刻 `break`。

### 顺带量到的一个审计盲区（未修）

门禁只管**重叠**，不管"**快贴上**"。实测收敛结果里
`「quenched jet」↔「Surface bias:」` 只隔 **0.9pt**，视觉上已经贴住了，
但审计判 ✅。同一份图里另有 23 对 <6pt 的间隙是**行内正常词距**，
所以不能简单把阈值一调了事 —— 要区分"行内词距"和"行间过近"。

---

## v1.3 — 2026-09-19

「草图 → 卡通图」流程的剩下两半：**IR 可执行化**（以前 IR 是散文，无法自动化）
与**创作任务的自动收敛**（以前 auto_converge 必须给参考图，草图为输入时完全走不通）。

### 新增

- **`scripts/ir_to_scene.py`** —— IR → 可运行构图骨架
  - IR 的 `primitive` 改用 `cartoon_lib.PRIMITIVES` 注册名，
    `params` 从中文散文（`"约 0.34 画布宽"`）改成**归一化数值**
  - `--check` 查三类问题：primitives 不在注册表 / params 是散文 /
    **参数名不在该图元签名里**（实测：`jet` 签名是 `(x0,y0,x1,y1)`，
    IR 写成 `(x1,y1,x2,y2)`，生成出来会 TypeError）
  - 单位**按图元声明**（`PARAM_UNITS`），不能按参数名一刀切 ——
    实测 `ry` 在 `qgp_blob` 里是扁平比、在 `lorentz_nucleus` 里是纵向半径，
    一刀切会生成 `ry=499.2` 这种垃圾
  - 参考实现：`ir/sketch3_jet.exec.ir.yaml`（`--check` 通过，骨架能直接跑出图）

- **`auto_converge.py --profile`** —— 创作任务的自动收敛
  - 目标从"某张参考图"换成**风格档案的区间**，判据与 `delivery_gate` 一致
  - 被驱动脚本可导出 `RULES` 覆盖内置映射表（映射表是"这张图的专家知识"）

### ★ 实测推翻了一个假设

`cartoon_lib` 的默认值原本想靠"加线框/纹理"提高 `edge_density`。
敏感度实测（`sketch3_to_figure.py --sensitivity`）：

| 参数 | whitespace | edge_density | dark_ratio | saturation |
|---|---|---|---|---|
| `scale` | −26% | 8% | 23% | **41%** |
| `detail`（线框/纹理） | −0.2% | **1.9%** | 3.3% | 0.3% |
| `label_scale` | −1.9% | **+99%** | **+387%** | −7.7% |
| `plate` | −2.6% | −0.0% | −0.2% | −11% |

**线框/纹理是死杠杆（1.9%）**，而 `dark_ratio` 与 `edge_density` 的真正杠杆是**字号**。
加网格那个假设不成立，已从 `RULES` 里拿掉。

### ★ 自动收敛会"作弊"—— 已加保护

拿 `--profile` 驱动卡通图时，优化器为了满足 `dark_ratio`/`edge_density`，
把 `label_scale` 一路推到上界，结果 **"nucleus A" 与 "nucleus B" 挤成不可读的一团、
底部说明文字溢出画布**。机械上收敛了，图上更差了 —— 正是纪律 3 禁止的
"优化指标而不是优化图"。

对策：创作任务下 `dark_ratio` / `edge_density` **默认不参与优化**
（`INK_PROXY_METRICS`），它们只作诊断。要强行优化得显式加 `--include-ink-metrics`。

### 修复

- `ir/sketch3_jet.ir.yaml` **不是合法 YAML**（引号标量后直接跟中文括号），
  这会让 `delivery_gate --ir` 直接崩掉。已修，两份副本都修了。
- `delivery_gate.check_ir` 现在对**格式错误的 IR 降级为提醒**而不是炸掉整道门禁
  （IR 断言本来就只打印、不代判）。

### 库的两个 bug（由子任务盘点时发现，已修）

1. **`light_cone` 画的是一条直线，不是 V**。原实现 `y2 = oy - sgn*L*slope`
   让 `sgn` 吃掉了负号 → 两支分别落在 t>0 和 t<0 两侧，连起来是**过原点的直线**。
   实测：`light_cone(300,300,120)` 给出端点 (180,420) 与 (420,180)，与原点共线。
   对靠画布底部的图，其中一支会直接出界。已改成两支都 `oy - L*slope`。
2. **`proper_time_family` 的 z 采样范围写死 ±2.2τ**，没有参数 → 全支的左分支
   会横穿相邻面板。已加 `z_span` 与 `half` 参数。
   顺带更正：原来画的其实是 `rx=τ, ry=1.32τ` 的**四分之一椭圆**，不是 τ=const 曲线；
   真正的 τ=const 在 t–z 平面上是双曲线 t=√(τ²+z²)。

### ★ 又一次"检查看着真、其实抓不到"

`sketch4` 重写后的光锥核对**只查了 |斜率| = 1**，没查朝向。
注入测试（把一支改成向下）**照样通过** —— 因为斜率集合仍是 {+1,−1}，
而"过原点的直线"正好也是这个集合。已补上"两支都必须在 t>0 侧"。

这条和 v1.2 那个"自证型测试"是同一类问题：
**一个命名正确的检查，不一定检查了它名字里的那件事。**

### 子任务产出（sketch1/2/4 改用图元库）

- 三个脚本里 `^def (库图元名)` 现在都是 **0** —— 重复定义清空
  （`sketch_to_figure` 删 86 行，`sketch4_to_figure` 删 56 行）
- `sketch4` 的假几何核对（`±224/224` 恒真断言、重抄常量）改成**从存盘 SVG 读实际坐标**，
  并已用注入法验证非恒真
- 修掉一个**原有**问题：`timeline.pdf` 原本被构图审计阻断（「τ=1」×「pre-eq.」重叠 72%）
- 诚实标注：sketch1/2 的核颜色变了 —— 旧本地 `shaded_sphere` 的 `mix()` 不做 clamp，
  `#6a7fd0 × 1.35` 溢出成 7 位 hex 被截断成黄绿色。库做了 clamp，是**修 bug 不是退步**

### 工具链

新增 `cartoon_lib.py`（18 图元）与 `ir_to_scene.py`，已进 ChatGPT 包
（工具包从 27 个文件 → 29 个，自检仍全绿）。

---

## v1.2 — 2026-09-19

「草图 → 卡通图」这条线：从"每张重写脚本"改成图元库驱动，并修掉一个
**误杀了 4/5 张草图产出**的门禁缺陷。

### 新增

- **`scripts/cartoon_lib.py`** —— 卡通示意图图元库（18 个图元 + `PRIMITIVES` 注册表）
  - 合并自三份一次性脚本：`label` 3→1、`lorentz_nucleus` 2→1、
    QGP 团块 2→1（原来两份约 85% 逐行相同）
  - 统一调色板：同一个物理概念（冷色核）原来在两张图里用了**两套不同的蓝**
  - 顺带修掉 4 个真 bug：`shift=0` 时同一椭圆画两遍、三个 `label` 写死
    `DejaVu Sans` 导致**中文静默变豆腐块**、`jet` 的 52px 画布魔数、
    `tapered_arrow` 箭头长 = 箭杆长 30%
  - 载体脚本从 296 行降到约 110 行（只写构图，不写图元）

### 修复 —— ★ 门禁误杀（本轮最重要的一条）

`delivery_gate.py` 的升级规则「advisory 指标偏离 >50% → 升级为阻断」
与本文件开头写的原则（**指标在类内一致，才配当阻断门**）直接冲突。

实测数据（T3 插画型档案各指标的类内相对 IQR）：

| 指标 | relIQR | 项目自己的分级 |
|---|---|---|
| whitespace | 0.140 | 一致 → 阻断 ✅ |
| dark_ratio | 0.277 | 分散 → 应仅提醒 |
| edge_density | 0.389 | 分散 → 应仅提醒 |
| saturation | 0.539 | 很散 → 应仅提醒 |

四个 advisory 指标**全部**达不到"一致"标准，却**全部**能被升级成阻断门。
后果：一张卡通示意图的 `dark_ratio = 0.0063` 被判「超出 76%、阻断」——
而**真实期刊图 T3-08 的 dark_ratio 正好也是 0.0063**，8 张 T3 精选里有 1 张比它更暗。
**5 张草图产出有 4 张是这样被误杀的。**

修法：升级前先查该指标的类内离散度（`CONSISTENT_IQR = 0.15`），飘的指标没资格卡人。

⚠️ 顺带发现：`dark_ratio` 与 `text_area_ratio` **几乎是同一个函数**
（近黑像素占比，阈值 0.35 vs 0.30）。它测的是**文字密度**，不是画风深浅。
之前"卡通图太淡要加深描边"的判断是错的——加粗描边对它的影响可以忽略。

### 修复 —— 范畴错误改用范畴判据

上一条修完，灰度图会溜过门禁（`saturation=0` 只触发提醒），
而这正是升级规则原本要防的漏洞。但**"黑白图"是范畴错误，不是统计偏离**，
不该靠调统计指标的门槛来抓。新增 `chroma_mean()` 直接判彩度：
彩色目标 + 候选彩度 ≈ 0 → 阻断。

### 修复 —— 构图审计的压字判据

`audit_composition.py` 的判据原为 `thin = (宽<3 or 高<3)`——**只认细线**，
色带/箭头/半透明块压字一律查不出。改成**量可读性本身**：
把文字 bbox 渲染出来，最暗的一撮像素当笔画、其余当背景，比对比度。

（诚实交代：一度以为这修的是"hard scattering 被喷注压住"那个案例。
量下来它的对比度是 **0.929**，完全可读——那个问题是**摆放不美观**，
不是**读不出来**。新判据抓的是后者。摆放问题属于另一类，暂未纳入阻断。）

### 修复 —— 自证型测试

`test_escalation` 原来**自己重写了一遍阈值逻辑**（本地定义 `verdict()`），
测的是"我抄的这份对不对"而不是"真代码怎么做"，改了真代码它照样绿。
改成真的 subprocess 跑 `delivery_gate.py`，两种情形都验
（范畴错误必阻断 / 飘的指标必不阻断），并已验证**回退修复会让它失败**。

---

## v1.1 — 2026-09-19

为 ChatGPT 落地做打包时，暴露出 10 处**"本地能跑、目标环境崩"**的缺陷，
全部修复。本地 13/13 回归仍全过；并已验证在**扁平沙箱布局**下也 13/13。

### 修掉的缺陷

| # | 位置 | 问题 | 修法 |
|---|---|---|---|
| 1 | `svg_lib.py` | `FONT_CJK = "SimHei"` 写死。沙箱只有 Noto Sans CJK 且 cairosvg 不做字体回退 → **中文全变豆腐块**；而"数墨迹像素"的自检抓不到（豆腐块也有墨） | 改为运行时探测（`fc-list` → matplotlib 字体表），挑第一个真装了的 |
| 2 | `geom.py` | `blob()` 无条件 `return Polygon(pts)`，无 shapely 时报 `NameError`（指不到"缺 shapely"）。`demo_timeline`/`demo_jet_quenching` 都走这条路 | 加纯 Python 环形多边形替身（输出与 shapely **逐字节相同**）；三个布尔函数改为明确报错 |
| 3 | `check_tools.py` | 探测表**缺 shapely 和 PyYAML**——两者都会被 import。于是"缺工具"报告永远误导性地全绿 | 补进表里；并新增 `substitute` 字段 |
| 4 | `check_tools.py` | "缺工具=去装"在无网络沙箱里是死路 | 缺失项同时给【装不了时的替代路径】；结尾提示不要建议安装 |
| 5 | `delivery_gate.py` | `check_ir` 缺 PyYAML 直接 ModuleNotFoundError | 降级为跳过并说明（该函数本来就只打印、不代判，跳过无损） |
| 6 | `delivery_gate.py` | 单张参考图时 `p25=p75=该值`（区间塌成一点），而 `whitespace` 是**阻断项** → **阻断门 + 跑不动收敛 = 死锁** | 用**最近子类档案**兜底区间 + ±15% 地板；并列时提示可用 `--class` 指定；拿不到档案时阻断自动降级为提醒 |
| 7 | 5 处 | 陈旧 `sys.path`：`auto_converge` / `repro_T3-03_param` / `demo_combined` / `demo_upc_color` | 改为按脚本自身位置定位（扁平布局也适用） |
| 8 | `demo_combined.py` | 依赖 `pdflatex`/`pdftops`，沙箱没有 → 跑到一半才失败且报错干瘪 | 前置检查 + 说明替代（公式改用 Unicode 直写），卡通部分照常跑 |
| 9 | `selftest.py`（旧 ChatGPT 包内） | **无论多少项 FAIL 都 exit 0**；且只检查 4 个脚本、完全不检查门禁层 → 全绿但误导 | 新 `selfcheck.py`：失败即 exit 1，并加**真·豆腐块检测**（渲染「中」「文」比对位图）+ 哈希校验 + 挂载探测 |
| 10 | `evals/test_tools.py` | 硬假设 `evals/` 与 `scripts/` 是兄弟目录（扁平沙箱首个失败在 `:139`）；`tool_detection_gives_install_cmd` 是假绿 | 自动适配两种布局 + `HEPNF_SCRIPTS` 覆盖 + 打印实际路径与哈希；补两条真断言 |

### 新增

- `evals/test_tools.py` 运行开头打印**工具目录、判定依据、各模块 sha256**——
  解包出错/测到旧副本时能立刻看出来
- 打包层（在 `HEP/skills/chatgpt/`，不属于本仓库）：
  `build_knowledge.py` 从本 skill 重新生成 Knowledge 包 + BUILD_ID，
  杜绝"手抄打包必然漂移"

### 教训

**这 10 处有一个共同点：在开发机上全都不会暴露。**
`sys.path[0]` 兜住了坏路径、本机装了 shapely/PyYAML/SimHei/pdflatex。
换到目标环境才一起爆出来。→ **打包到目标平台本身就该当成一轮测试。**

---

## v1.0 — 2026-09-19

首个版本。从零构建，全部结论来自实测。

### 核心方法
- **六层 IR**（物理 / 元素 / 构图 / 风格 / 执行 / 验收）+ **geometry_constraints 层**
- **按图选后端**：svg_lib / matplotlib / TikZ / Blender / ROOT / 多工具联合
- **两道门禁**：全局风格收敛 + 局部构图审计

### 关键的事实测结论（都写进了 SKILL.md 的硬纪律）
1. **几何量必须量，禁止看** —— 目测径向线 48 条，脚本实测 40 条
2. **绘制顺序就是 z 序** —— 画反了元素凭空消失且不报错
3. **风格指标是诊断工具，不是优化目标** —— 整体调会把不该动的地方也调坏
4. **物理正确性无法自动验证** —— 只能靠可判定的几何约束 + 人看
5. **不要把版权受限的图打包发布** —— 推之前逐张查许可

### 工具（18 个）
生成类：`svg_lib` `geom` `demo_combined` `demo_jet_quenching` `demo_timeline` `demo_upc_color`
验证类：`delivery_gate` `audit_composition` `audit_panels` `check_delivery` `check_render` `style_bench` `style_profile`
流程类：`check_tools` `auto_converge` `compare_ref` `assemble_panels` `extract_figures`

### 评测
- `evals/test_tools.py` —— 13 个机器可跑的回归测试，每个对应一个真实踩过的坑
- `evals/evals.json` —— 14 个行为评测 case，27 条可勾选断言

### 风格档案
T1 / T2 / T3 / T3-插画 / T3-线稿 / 未分类 —— **只存测量数字，不含原图**（版权干净）

---

## 踩过的坑（按发现顺序）

| # | 坑 | 教训 |
|---|---|---|
| 1 | PyMuPDF 渲染 SVG → 渐变全黑 | 必须用 cairosvg |
| 2 | cairosvg 不做字体回退 → 中文静默丢失 | font-family 只写一个存在的字体 |
| 3 | 无单字体兼有中文+Unicode 下标 | 按字符集分字体 |
| 4 | `baseline-shift` 被忽略 / 百分比字号解析错 | 用 dy + 绝对字号 |
| 5 | `text-anchor:middle` + tspan → 锚点算错 | 居中文案用 Unicode 下标 |
| 6 | 水平线的 linearGradient 退化 → 箭杆不渲染 | 细长线不能用渐变描边 |
| 7 | `markerUnits` 默认随线宽缩放 → 粗箭头长巨三角 | 改 userSpaceOnUse |
| 8 | 复合图的矢量验证不能用 `get_drawings()` 计数 | 面板是 XObject，看 `get_images()==0` |
| 9 | 指标对分辨率/出处敏感 | color_richness、gradient_ratio 不可跨来源比 |
| 10 | 拿 2-panel 参考图比 1-panel 复现图 | 比对前先确认同范围 |
| 11 | 归一化不对称（大图被降采样） | 归一到同一像素尺寸再测 |
| 12 | 门禁偏离度量选错分母 | 用区间边界，不用区间宽度 |
| 13 | 类没拆对 → "修一个坏一个" | T3 要分插画型/线稿型 |
| 14 | 全局指标测不到局部问题 | 必须加构图审计 |
